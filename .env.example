# --- Database ---
POSTGRES_DB=kittygram
POSTGRES_USER=kittygram_user
POSTGRES_PASSWORD=kittygram_password
DB_HOST=db
DB_PORT=5432

# --- Django ---
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# --- CORS (only needed when running the frontend dev server separately) ---
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:9000,http://127.0.0.1:9000
